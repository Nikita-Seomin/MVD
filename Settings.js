let dbConfig = {
    host: "localhost",
    user: "root",
    database: "MVDBD",
    password: "root"
}
module.exports.dbConfig = dbConfig;

let config = {
    "secret" : "KEY",
    "sold" : 7
}
module.exports.config = config;