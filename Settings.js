let dbConfig = {
    host: "localhost",
    user: "root",
    database: "MVDBD",
    password: "root"
}
module.exports.dbConfig = dbConfig;

let config = {
    "secret" : "KEY"
}
module.exports.config = config;